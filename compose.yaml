services:
  frontend:
    build:
      context: .
      dockerfile: dockerfile
    container_name: crud-frontend
    ports:
      - "8080:80"
    volumes:
      - ./Frontend:/usr/share/nginx/html
    restart: always